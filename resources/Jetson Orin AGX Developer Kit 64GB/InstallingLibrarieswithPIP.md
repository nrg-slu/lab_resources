# Enable PIP 

In order to install libraries make sure that the most recent version of jetson container is installed. 
```bash
cd jetson-containers 
git pull
```
Then you will be able to use pip inside the container. 